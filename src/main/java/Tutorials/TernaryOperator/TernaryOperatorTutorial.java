package Tutorials.TernaryOperator;

public class TernaryOperatorTutorial {
    public static void main(String[] args) {
        
        // Example 1
        // int score = 70;
        // score = 44;

        // System.out.println((score >= 45) ? "You have passed! 😃😃" : "You have failed! 😭😭");
        // (condition) ? (statement if condition true) : (statement if condition false)

        // Example 2
        // int number = 3;
        // number = 99213134;

        // System.out.println((number % 2 == 0) ? "The number is even" : "The number is odd");

        // Example 4
        // int militaryTimeHours = 15;
        // militaryTimeHours = 10;

        // System.out.println((militaryTimeHours < 12) ? "A.M." : "P.M.");

        // Example 5
        int income = 70000;
        income = 39900;

        System.out.println((income >= 40000) ? 0.25 : 0.15);
    }
}
